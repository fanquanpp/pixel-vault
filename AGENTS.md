# AGENTS.md — 项目约束 / Project Constraints

## 输出约束（最高优先级 / highest priority）

- 任何输出一律禁止使用 emoji。适用范围：代码、注释、文档、README、提交信息、PR 描述、UI 文案、日志、网页内容。
- 不使用 emoji 及其变体选择器（U+FE0F），包括对勾、警告、星形、三横线等易渲染为 emoji 的符号（如 U+2713、U+26A0、U+2605、U+2630）。
- 允许的少量功能符号：纯文本箭头与数学符号（← → ↓ × ≡）。不确定时一律不用。

English: Never use emoji in any output (code, comments, docs, commit messages, PR descriptions, UI copy, logs, web content). Only plain-text arrows/operators (← → ↓ × ≡) are permitted; when in doubt, use none.
